let allWords = ["cero","rien","zero","uno","un","one","dos","deux",    "two"]
let spanishFrenchEnglishWords = [ ["cero","rien","zero"], ["uno","un","one"], ["dos","deux","two"] ]

export const solution = allWords
export const problem = spanishFrenchEnglishWords

// INSTRUCTIONS:
// collect all the words for each number, in every language, in a single, flat list
//const allWords = [0,1,2]
//  .concatMap(function(index) {
//    return spanishFrenchEnglishWords[index]
//  })
